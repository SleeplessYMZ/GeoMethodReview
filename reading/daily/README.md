# Daily Reading

本目录保存由 `scripts/prepare-daily-reading.mjs` 生成的每日方法文献精读包。

生成示例：

```bash
node scripts/prepare-daily-reading.mjs --count 3
```

如需尝试下载开放获取 PDF：

```bash
node scripts/prepare-daily-reading.mjs --count 3 --download-open-access
```
